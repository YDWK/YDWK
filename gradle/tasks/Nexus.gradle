import java.time.Duration

nexusStaging {
    packageGroup = 'io.github.realyusufismail'
    serverUrl = "https://s01.oss.sonatype.org/service/local"
    username = project.hasProperty('mavenUsername') ? project.property('mavenUsername') : 'username'
    password = project.hasProperty('mavenToken') ? project.property('mavenToken') : 'password'
}